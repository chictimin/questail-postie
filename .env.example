# 필수는 아님. 없음으면 로컬 폴백 또는 요약 스킵 모드로 동작한다.
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_API_KEY=
MODEL=gpt-4o-mini

# 디스코드 발행용. 없으면 output/latest.md 저장만 수행한다.
DISCORD_WEBHOOK_URL=

# Steam 키는 전역 설정(~/.config/questail/.env)에서 읽기만 한다. 여기에 적지 않는다.
# 뉴스 수집(공개 API·RSS)에는 키가 필요 없다.
